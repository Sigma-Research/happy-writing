var tool = {
    getSectionStudyState: function (array) {
        var state = true
        array.forEach(function (item) {
            if (!item) state = false
    })
        return state
    },
}
// 导出对外暴露的属性
module.exports = {
    getSectionStudyState: tool.getSectionStudyState
}
